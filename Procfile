web: node app.js
// Create a file called Procfile inside the folder project.
// It tells heroku wich file contains or server code.
// Thi is the file that Heroku will look at to launch the website.
// Declares that this process type will be attached to the HTTP routing stack of Heroku, and receive web traffic when deployed.
